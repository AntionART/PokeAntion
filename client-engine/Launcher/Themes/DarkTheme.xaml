<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:Launcher.Converters">

    <conv:ProgressFillConverter x:Key="ProgressFillConverter"/>

    <SolidColorBrush x:Key="BgBrush" Color="#181A21"/>
    <SolidColorBrush x:Key="TitleBarBrush" Color="#121319"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#32343E"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#E6392B"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#F04B3C"/>
    <SolidColorBrush x:Key="TextBrush" Color="#E6E6EB"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#8C8F9B"/>
    <SolidColorBrush x:Key="TrackBrush" Color="#282B36"/>
    <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#3C3F4A"/>

    <!-- Barra de progreso propia: la de WPF por defecto trae el look "Windows clásico" (marquee
         gris a bloques) que rompe con cualquier intento de identidad visual propia. -->
    <Style x:Key="AccentProgressBar" TargetType="ProgressBar">
        <Setter Property="Height" Value="10"/>
        <Setter Property="Background" Value="{StaticResource TrackBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid x:Name="Track" ClipToBounds="True">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                        <Border x:Name="Indicator" Background="{TemplateBinding Foreground}" CornerRadius="5"
                                HorizontalAlignment="Left" RenderTransformOrigin="0,0">
                            <Border.RenderTransform>
                                <TranslateTransform x:Name="IndicatorOffset"/>
                            </Border.RenderTransform>
                            <Border.Width>
                                <MultiBinding Converter="{StaticResource ProgressFillConverter}">
                                    <Binding Path="Value" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="Maximum" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    <Binding Path="ActualWidth" ElementName="Track"/>
                                </MultiBinding>
                            </Border.Width>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsIndeterminate" Value="True">
                            <Setter TargetName="Indicator" Property="Width" Value="90"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard Name="MarqueeStoryboard">
                                    <Storyboard>
                                        <!-- To fijo (no bindeado): un Storyboard con un binding activo no puede
                                             sellarse para compartirse entre threads/instancias de Style, y esta
                                             ventana tiene ancho fijo (ResizeMode=NoResize) así que no hace falta. -->
                                        <DoubleAnimation Storyboard.TargetName="IndicatorOffset" Storyboard.TargetProperty="X"
                                                          From="-90" To="1000"
                                                          Duration="0:0:1.4" RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="MarqueeStoryboard"/>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox con watermark (WPF no trae placeholder nativo): Tag = texto a mostrar cuando
         Text está vacío, oculto automáticamente apenas el usuario empieza a tipear. -->
    <Style x:Key="WatermarkTextBox" TargetType="TextBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4"/>
                        <TextBlock Text="{TemplateBinding Tag}" Foreground="{StaticResource MutedTextBrush}"
                                   FontSize="{TemplateBinding FontSize}" Margin="8,0,0,0" VerticalAlignment="Center"
                                   IsHitTestVisible="False" Opacity="0.7">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text.Length, RelativeSource={RelativeSource TemplatedParent}}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FlatButtonBase" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Height" Value="34"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Opacity" Value="0.88"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
        <Setter Property="Background" Value="{StaticResource SecondaryButtonBrush}"/>
    </Style>

</ResourceDictionary>
