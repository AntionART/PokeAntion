# Herramientas/instaladores locales (no son parte del proyecto)
/go1.26.5/
/go1.26.5.windows-amd64.zip
/postgresql-16.5/
/postgres_data/
/postgresql.log

# ROMs con copyright (Nintendo/Game Freak) - nunca subir
*.gba
*.gbc
*.gb

# Carpeta de ROMs comprimidas (mismo motivo que arriba - *.gba no cubre estas .zip)
/Pokemon Rom/

# Binarios compilados / build artifacts
bin/
obj/
node_modules/
*.exe
*.exe~
server/pokemon-online-server
*.log

# Bundle del cliente armado por scripts/build-client-bundle.ps1 (generado, ~50MB) - el
# servidor lo sirve desde CLIENT_BUNDLE_PATH, no hace falta versionarlo en git
/client-bundle.zip

# Backups de la base generados por scripts/backup-database.ps1 - datos reales de cuentas/
# personajes, nunca subir a git
/database/backups/

# Editor / OS
.vs/
.idea/
Thumbs.db
.DS_Store
